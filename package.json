{
  "name": "cssspecificitygraph",
  "version": "0.0.0",
  "description": "Generates a CSS Specificity Graph for your CSS.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "CSS",
    "css",
    "specificity",
    "graph"
  ],
  "author": "Jonas Ohlsson",
  "license": "MIT",
  "dependencies": {
    "css-parse": "^2.0.0",
    "cssbeautify": "^0.3.1",
    "d3": "^3.4.13",
    "specificity": "^0.1.4"
  },
  "devDependencies": {
    "browserify": "^6.2.0"
  },
  "scripts": {
    "example": "browserify example.js -o dist/bundle.js && open index.html"
  }
}
